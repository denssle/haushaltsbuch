import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'haushaltsbuch',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
